import 'package:flutter/material.dart';

class NewPage extends StatelessWidget {
  int x;
  String y;
  NewPage({super.key, required this.x, required this.y});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}